---
layout: post
title: "Git commands"
date: 2025-10-20 00:33:00 +0000
categories: [IT]
tags: [git]
author: "JayLL"
---

## help me [install](https://git-scm.com/install/linux.html) git & gh

🫵 [Windows installation](https://cli.github.com/)

``` bash
sudo apt update
# install git
sudo apt-get install git
# install gh
sudo apt install gh
# login
gh auth login
```

## I am traippn, I want to config username & email
```bash
git config --global user.name "jayll1303"
git config --global user.email "traippnse161809@fpt.edu.vn"
```

## How to add changes to last commit without making new one

```bash
# stash change
git add .
# add to last commit
git commit --amend --no-edit
# rewrite commit history
git push --force-with-lease
```

## I am working at company with policies, help me skip SSL

### Setup SSL for local repo  (not recommended but I like)

```bash
git config http.sslCAInfo "/path/to/your/ca.pem"
```

### Ignore SSL in single command

Add ```git -c http.sslVerify=false``` to command, for example:

```bash
git -c http.sslVerify=false push -u delta --all
```

## I want to delete branch

### List branch

```bash
git branch
```

### Delete remote branch

```bash
git push origin -d branch_name
```

### Delete local branch

```bash
git branch -D branch_name
```

## I want to stash changes with message

```bash
git stash push -m "message"
```